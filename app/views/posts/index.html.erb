<div class="container">
  <%= link_to "New Post", new_post_path if policy(Post).new? %>
  <h1>Posts</h1>
  <% @posts.each do |post| %>
    <div class="cards">
      <%= post.title %>
      <%= post.user.user_name %>
      <%= post.content %>
      <%= post.photo %>
    </div>
  <% end %>
</div>
