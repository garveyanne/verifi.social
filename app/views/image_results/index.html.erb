<div class="container">
  <h1>Results</h1>
  <% @results.each do |result| %>
    <div class="cards">
      <%# here is where the image will go but cloudinary isnt working atm %>
      <%# when user clicks the image, they jump to the show page %>
    </div>
  <% end %>
</div>
